# CONTRIBUTING, "What may and may not change in a pull request", is this repository's process
# in prose: wording, structure and annotations are open to argument, and the decided
# architecture is not changed by a pull request that quietly reverses it. The gates check the
# mechanical half — the required banner, the `[COUNSEL: …]` form, the links, the canonical
# render. The half they cannot check is whether a diff to the text still says what the
# decisions say. That read is a human's, and the paths below are where it is owed.
#
# Dated note, 2026-09-11: this file is new on this date. The owner is the organisation TEAM
# `@purposesource/stewards` (id 19450495, created 2026-09-11T11:42:17Z, two members), read back
# read-only the same day. The members are not listed here: neither is a PUBLIC member of the
# organisation (`orgs/purposesource/public_members` → `[]` on this date), and this is a public
# repository, so the count is recorded and the logins stay in the private record.
#
# WHAT THIS FILE DOES NOT DO YET, measured read-only on 2026-09-11. Two operator acts remain,
# IN THIS ORDER, and until the first of them these rows request review from nobody:
#
#   1. THE TEAM HOLDS NO REPOSITORY ACCESS. `orgs/purposesource/teams/stewards/repos` returns
#      `[]`, and the team object reports `repos_count: 0`. GitHub resolves a TEAM as a code
#      owner only where that team has write access to the repository — even when every member
#      already has write individually. Granting `@purposesource/stewards` WRITE on `license`,
#      `registry-v0` and `spec` is the first act.
#   2. NO BRANCH CARRIES A REQUIRED REVIEW. `repos/purposesource/license/branches/main/
#      protection` reports `required_pull_request_reviews: null` — linear history, no
#      force-push, no deletions, and nothing else. "Require a pull request before merging" with
#      "require review from Code Owners" (1 approval) is the second act. Until it exists this
#      file is advisory: it requests a review, it does not require one.
#
# Act 2 before act 1 produces a rule nobody can satisfy, and that locks the admins out of their
# own repository. Grant the access, confirm on the next pull request that a review is actually
# requested of `@purposesource/stewards`, and only then require it. Both acts are the
# operator's, tracked as PS-O14.
#
# NOTHING HERE IS COUNSEL-ISSUED, and a code owner is not a lawyer. A review requested by this
# file is an editorial and architectural read against the recorded decisions; it is not legal
# review, and it does not make a draft operative. That remains what README "Status" says it is.
#
# NOT LISTED, and not by oversight: `scripts/`, `decisions/`, `ANNOTATIONS.md`, `FAQ-OSPO.md`
# and `CONTRIBUTING.md`. The record scoped this file to the text paths and the pattern file,
# and widening it is a later decision rather than a detail of this one.

# The drafts. The licence text itself and the contributor sign-off — the two documents whose
# wording IS the product of this repository.
/drafts/                @purposesource/stewards

# The canonical rendering. It is generated from the draft, and a hand-edit here is a divergence
# between the text people read and the text the record ratified.
/canonical/             @purposesource/stewards

# The substitution table. Every placeholder the texts resolve through: a change here rewrites
# every document at once without touching one of them.
/placeholders.json      @purposesource/stewards

# The pattern file and the required banner. Changing what the copy gate forbids is changing
# what may be said about the licence, which is never a mechanical decision.
/copy-lint/             @purposesource/stewards

# The gates and this file. A CODEOWNERS that can be edited without its owner's review reviews
# nothing the next day.
/.github/               @purposesource/stewards
